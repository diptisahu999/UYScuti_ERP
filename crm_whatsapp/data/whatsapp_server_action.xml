<odoo>

    <!-- ✅ WhatsApp for CRM Lead -->
    <record id="action_whatsapp_send" model="ir.actions.server">
        <field name="name">Send WhatsApp</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="binding_model_id" ref="crm.model_crm_lead"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
for rec in records:
    rec.send_whatsapp_message()

action = {
    "type": "ir.actions.client",
    "tag": "reload",
}
        ]]></field>
    </record>

    <!-- ✅ WhatsApp for Sale Orders -->
    <record id="action_whatsapp_send_saleorder" model="ir.actions.server">
        <field name="name">Send WhatsApp</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
for order in records:
    order.action_send_whatsapp()

action = {
    "type": "ir.actions.client",
    "tag": "reload",
}
        ]]></field>
    </record>

    <!-- ✅ WhatsApp for Contacts -->
    <record id="action_whatsapp_send_partner" model="ir.actions.server">
        <field name="name">Send WhatsApp</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
for rec in records:
    rec.send_whatsapp_message()

action = {
    "type": "ir.actions.client",
    "tag": "reload",
}
        ]]></field>
    </record>

    <!-- ✅ WhatsApp for Project -->
    <record id="action_whatsapp_send_project" model="ir.actions.server">
        <field name="name">Send WhatsApp</field>
        <field name="model_id" ref="project.model_project_project"/>
        <field name="binding_model_id" ref="project.model_project_project"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
for rec in records:
    rec.send_whatsapp_message()

action = {
    "type": "ir.actions.client",
    "tag": "reload",
}
        ]]></field>
    </record>


        <!-- ✅ WhatsApp for Customer Invoices -->
    <record id="action_whatsapp_send_invoice" model="ir.actions.server">
        <field name="name">Send WhatsApp</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
for invoice in records:
    invoice.action_send_whatsapp_invoice()

action = {
    "type": "ir.actions.client",
    "tag": "reload",
}
        ]]></field>
    </record>

</odoo>
