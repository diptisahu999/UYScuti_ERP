<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_payment_form_advance" model="ir.ui.view">
        <field name="name">account.payment.form.advance</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="mode">primary</field>
        <field name="priority">1000</field>
        <field name="arch" type="xml">
            <xpath expr="//header" position="replace">
                <header invisible="1"/>
            </xpath>
            <xpath expr="//sheet" position="after">
                <footer>
                    <button string="Save" special="save" class="btn-secondary"/>
                    <button name="action_post" string="Confirm" type="object" class="btn-primary" data-hotkey="q"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </xpath>
        </field>
    </record>
</odoo>
